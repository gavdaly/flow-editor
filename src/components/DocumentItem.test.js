import React from 'react'
import DocumentItem from './DocumentItem'

import { shallow } from 'enzyme'

let wrapper

describe('<DocumentItem>', () => {
  beforeEach(() => { wrapper = shallow(<DocumentItem />) })

  it('')
})
